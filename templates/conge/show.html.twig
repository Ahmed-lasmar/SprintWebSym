{% extends 'RRH.html.twig' %}

{% block title %}Conge{% endblock %}

{% block body %}
    <h1>Conge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcon</th>
                <td>{{ conge.idcon }}</td>
            </tr>
            <tr>
                <th>Idper</th>
                <td>{{ conge.idper }}</td>
            </tr>
            <tr>
                <th>Ddepot</th>
                <td>{{ conge.ddepot ? conge.ddepot|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Typedemande</th>
                <td>{{ conge.typedemande }}</td>
            </tr>
            <tr>
                <th>Etatdemande</th>
                <td>{{ conge.etatdemande }}</td>
            </tr>
            <tr>
                <th>Ddepart</th>
                <td>{{ conge.ddepart ? conge.ddepart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Dretour</th>
                <td>{{ conge.dretour ? conge.dretour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conge_index') }}">back to list</a>

    <a href="{{ path('app_conge_edit', {'idcon': conge.idcon}) }}">edit</a>

    {{ include('conge/_delete_form.html.twig') }}
{% endblock %}
