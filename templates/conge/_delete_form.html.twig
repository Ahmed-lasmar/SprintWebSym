<form method="post" action="{{ path('app_conge_delete', {'idcon': conge.idcon}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conge.idcon) }}">
    <button class="btn">Delete</button>
</form>
