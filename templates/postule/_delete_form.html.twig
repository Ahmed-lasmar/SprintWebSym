<form method="post" action="{{ path('app_postule_delete', {'idPost': postule.idPost}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postule.idPost) }}">
    <button class="btn">Delete</button>
</form>
