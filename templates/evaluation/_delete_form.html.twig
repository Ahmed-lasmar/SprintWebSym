<form method="post" action="{{ path('app_evaluation_delete', {'idEvaluation': evaluation.idEvaluation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation.idEvaluation) }}">
    <button class="btn">Delete</button>
</form>
