<form method="post" action="{{ path('app_employe_delete', {'idPer': employe.idPer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.idPer) }}">
    <button class="btn">Delete</button>
</form>
