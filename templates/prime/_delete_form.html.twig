<form method="post" action="{{ path('app_prime_delete', {'idPrime': prime.idPrime}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prime.idPrime) }}">
    <button class="btn">Delete</button>
</form>
