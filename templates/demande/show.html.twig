{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDem</th>
                <td>{{ demande.idDem }}</td>
            </tr>
            <tr>
                <th>IdPer</th>
                <td>{{ demande.idPer }}</td>
            </tr>
            <tr>
                <th>DateDepot</th>
                <td>{{ demande.dateDepot ? demande.dateDepot|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TypeDemande</th>
                <td>{{ demande.typeDemande }}</td>
            </tr>
            <tr>
                <th>EtatDemande</th>
                <td>{{ demande.etatDemande }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_index') }}">back to list</a>

    <a href="{{ path('app_demande_edit', {'idDem': demande.idDem}) }}">edit</a>

    {{ include('demande/_delete_form.html.twig') }}
{% endblock %}
