<form method="post" action="{{ path('app_formation_delete', {'idFor': formation.idFor}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idFor) }}">
    <button class="btn">Delete</button>
</form>
