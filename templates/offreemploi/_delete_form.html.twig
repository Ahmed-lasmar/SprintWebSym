<form method="post" action="{{ path('app_offreemploi_delete', {'idOffre': offreemploi.idOffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offreemploi.idOffre) }}">
    <button class="btn">Delete</button>
</form>
